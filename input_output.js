const numbers = [2, 1, 5, 7, 8]
const evenNumbers = numbers.filter(number => number % 2 === 0);
const oddNumbers = numbers.filter(number => number % 2 !== 0);

let numberSize = evenNumbers.length

let number_Size = oddNumbers.length

console.log([numberSize, number_Size])


const number = [4, 1, 5, 7, 9]
const even_numbers = number.filter(number => number % 2 === 0);
const odd_numbers = number.filter(number => number % 2 !== 0);

let number_size = even_numbers.length

let number__size = odd_numbers.length

console.log([number_size, number__size])







